export enum Modal {
  CONSOLE,
  NAVIGATOR,
  CATALOG,
  INVENTORY,
  PURSE,
  CLUB,

  ROOM_CREATOR,
}

export enum ModalNavigatorTab {
  HOTEL,
  ROOMS,
  ME,
  SEARCH,
}

export enum ModalInventoryTab {
  FURNITURE,
  FRAMES,
  PETS,
  RARES,
}

export enum Modal {
  CONSOLE,
  NAVIGATOR,
  CATALOG,
  INVENTORY,
  PURSE,
  CLUB,
}

export enum ModalNavigatorTab {
  HOTEL,
  ROOMS,
  ME,
  SEARCH,
}

export enum ModalInventoryTab {
  FURNITURE,
  FRAMES,
  PETS,
  RARES,
}

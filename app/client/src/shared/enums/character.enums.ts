export enum CharacterPart {
  BODY,
  ARM,
  HEAD,
}

export enum CharacterDirection {
  NORTH,
  WEST,
  FRONT,
  BACK,
  NORTH_WEST,
}

export enum CharacterBodyAction {
  IDLE,
  SIT,
  WALK_0,
  WALK_1,
  WALK_2,
  WALK_3,
}

export enum CharacterArmSide {
  RIGHT,
  LEFT,
}

export enum CharacterArmAction {
  IDLE,
  GRAB,
  DRINK,
  WAVE_0,
}
